#include "Chain_Stack.h"
#include<set>
#include<map>
#include<string>
/*int main()
{
	linkstack a;
	initstack(a);
	elemtype str[] = "abcd";
	for (int i = 0; i < strlen(str); i++)
	{
		cout << "在栈顶插入元素：" << str[i] << endl;
		push(a, str[i]);
	}
	traverse(a);
	cout << endl;
	elemtype e;
	for (int i = 0; i < 3; i++)
	{
		pop(a, e);
		cout << "删除的栈顶元素为:" << e << endl;
	}
	traverse(a);
	cout << endl;
	gettop(a, e);
	cout << "栈顶元素为：" << e;
	clear(a);
	destroy(a);
	return 0;
}*/